apiVersion: 1
datasources:
  - name: Prometheus
    # uid 固定：dashboard（lab-system.json）按此引用
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    jsonData:
      timeInterval: 15s
