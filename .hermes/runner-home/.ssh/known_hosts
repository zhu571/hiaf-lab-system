# github.com 主机公钥登记表（公钥非机密，可随仓库分发）。
# .gitconfig 默认 StrictHostKeyChecking=accept-new，首连自动信任（TOFU）；
# 注意：runner 容器内本目录为只读挂载，首连自动写入 known_hosts 会失败但连接
# 仅告警继续。持久化主机公钥需在宿主机执行：
#   ssh-keyscan -t ed25519,ecdsa,rsa github.com >> .ssh/known_hosts
